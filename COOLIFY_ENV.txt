# Coolify environment variables for hermes-nulab deployment.
# Set these in Coolify's dashboard for the application.

# ── Required ──────────────────────────────────────────────────────
# LLM providers — set both for profile switching, or just the one(s) you use
DEEPSEEK_API_KEY=sk-...
OPENAI_API_KEY=sk-...

# OpenPass vault passphrase (clean alphanumeric, no / + =)
OPENPASS_PASSPHRASE=...

# OpenPass MCP bearer token (must match vault's mcp-token file)
OPENPASS_MCP_TOKEN=...

# ── Gateway access ────────────────────────────────────────────────
GATEWAY_ALLOW_ALL_USERS=true

# ── WebUI ─────────────────────────────────────────────────────────
# Set in docker-compose.coolify.yml directly (not needed here):
#   HERMES_WEBUI_PASSWORD=Sillyauth2020?
#   HERMES_WEBUI_DEFAULT_WORKSPACE=/opt/data/workspace
#   WANTED_UID=501
#   WANTED_GID=20

# ── Do NOT set these (cause UID mismatch, now hardcoded in compose) ──
# HERMES_UID=1000  ← DO NOT USE — compose uses 501
# HERMES_GID=1000  ← DO NOT USE — compose uses 20

# ── Optional ──────────────────────────────────────────────────────
# API server (if you want OpenAI-compatible API access)
# API_SERVER_KEY=your-secret-key
# API_SERVER_HOST=0.0.0.0

# Messaging platforms (uncomment if using)
# TELEGRAM_BOT_TOKEN=...
# DISCORD_BOT_TOKEN=...
# SLACK_BOT_TOKEN=...
